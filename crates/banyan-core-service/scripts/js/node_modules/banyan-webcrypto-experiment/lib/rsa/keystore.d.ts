import KeyStoreBase from '../keystore/base.js';
import { KeyStore, Config, Msg, PublicKey } from '../types.js';
export declare class RSAKeyStore extends KeyStoreBase implements KeyStore {
    static init(maybeCfg?: Partial<Config>): Promise<RSAKeyStore>;
    sign(msg: Msg, cfg?: Partial<Config>): Promise<string>;
    verify(msg: string, sig: string, publicKey: string | PublicKey, cfg?: Partial<Config>): Promise<boolean>;
    encrypt(msg: Msg, publicKey: string | PublicKey, cfg?: Partial<Config>): Promise<string>;
    decrypt(cipherText: Msg, publicKey?: string | PublicKey, // unused param so that keystore interfaces match
    cfg?: Partial<Config>): Promise<string>;
    publicExchangeKey(): Promise<string>;
    publicWriteKey(): Promise<string>;
}
export default RSAKeyStore;
