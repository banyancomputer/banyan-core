import { EccCurve, KeyUse, PublicKey, SymmKey, PrivateKey, EscrowedKeyPair } from '../types.js';
/**
 * Generate a new ECC key pair
 * @param curve The curve to use
 * @param use The use of the key pair, either exchange or write
 */
export declare function genKeyPair(curve: EccCurve, use: KeyUse): Promise<CryptoKeyPair>;
/**
 * Import a public key from a base64 string
 * @param base64Key The base64 encoded public key
 * @param curve The curve to use
 * @param use The use of the key pair, either exchange or write
 */
export declare function importPublicKey(base64Key: string, curve: EccCurve, use: KeyUse): Promise<PublicKey>;
/**
 * Export a public key to a base64 string
 * @param publicKey The public key to export
 */
export declare function exportPublicKey(publicKey: PublicKey): Promise<string>;
export declare function fingerprintPublicKey(publicKey: PublicKey, curve?: EccCurve, hashAlg?: string): Promise<string>;
/**
 * Escrow an asymm key pair with a symmetric key using AES-GCM
 * @param keyPair The key pair to escrow
 * @param wrappingKey The symmetric key to use for wrapping -- This cannot be AES-KW
 * @param salt The salt to use for wrapping
 */
export declare function exportEscrowedKeyPair(publicKey: PublicKey, privateKey: PrivateKey, wrappingKey: SymmKey): Promise<EscrowedKeyPair>;
/**
 * Recover an escrowed key pair
 * @param publicKeyStr The public key to recover
 * @param escrowedPrivateKeyStr The wrapped private key to recover
 * @param unwrappingKey The symmetric key to use for unwrapping -- This cannot be AES-KW
 * @param salt The salt to use for unwrapping
 * @param curve The curve to use for the recovered key pair
 * @param use The use of the recovered key pair
 */
export declare function importEscrowedKeyPair(escrowKeyPair: EscrowedKeyPair, unwrappingKey: SymmKey, curve: EccCurve, use: KeyUse): Promise<CryptoKeyPair>;
declare const _default: {
    genKeyPair: typeof genKeyPair;
    importPublicKey: typeof importPublicKey;
    exportPublicKey: typeof exportPublicKey;
    fingerprintPublicKey: typeof fingerprintPublicKey;
    importEscrowedKeyPair: typeof importEscrowedKeyPair;
    exportEscrowedKeyPair: typeof exportEscrowedKeyPair;
};
export default _default;
